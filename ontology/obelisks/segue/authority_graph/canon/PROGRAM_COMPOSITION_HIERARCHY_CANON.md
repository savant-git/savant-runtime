PROGRAM_COMPOSITION_edifice_CANON.md ` `# PROGRAM COMPOSITION edifice CANON  STATUS: CANON AUTHORITY: CURRENT USER DIRECTIVE VERSION: 1.0.0 ACCEPTED_AT: 2026-08-14 OWNER: exile:modus SCOPE: SAVANT PROGRAM COMPOSITION AUTHORITY_EFFECT: SUPERSESSION  ## 1. Decision  The canonical Savant programming edifice contains exactly nine levels.  Ascending composition:  character → line → segment → snippet → script → engine → subsystem → system → application  This edifice is authoritative.  No alternate programming-level ordering may become competing authority.  ## 2. Cardinality  There are:  - 9 program levels; - 8 adjacent inter-level transitions.  The levels are:  1. character 2. line 3. segment 4. snippet 5. script 6. engine 7. subsystem 8. system 9. application  The adjacent composition transitions are:  1. character → line 2. line → segment 3. segment → snippet 4. snippet → script 5. script → engine 6. engine → subsystem 7. subsystem → system 8. system → application  A level and a transition are different primitives.  Nine levels therefore produce eight adjacent edifice transitions.  ## 3. Composition law  Each non-atomic level emerges through ordered composition of instances of the immediately preceding level.  Therefore:  - characters compose lines; - lines compose segments; - segments compose snippets; - snippets compose scripts; - scripts compose engines; - engines compose subsystems; - subsystems compose systems; - systems compose applications.  The edifice defines semantic composition.  It does not require each level to use a unique storage architecture.  ## 4. Instance law  Every stable program element is an instance.  Instances are reusable and graph-addressable.  Program substance is instantiated once and reused through composition.  Higher levels must not duplicate the substance of their children.  Every program instance must remain capable of exposing:  - identity; - level; - children; - lineage; - provenance; - dependencies; - dependents; - relationships; - Segues; - projections; - extensions; - future extensions.  ## 5. Segment law  `segment` is a distinct canonical level.  It exists between `line` and `snippet`.  A segment is not an alias for a line.  A segment is not an alias for a snippet.  Any programming edifice that projects:  character → line → snippet  without `segment` is obsolete where it purports to describe the current programming edifice.  Historical records containing the older formulation remain preserved as historical evidence.  ## 6. Segue law  Adjacent programming levels are connected through typed Segues.  The canonical adjacency set is deterministically derived from the nine-level edifice.  The eight adjacency Segues do not constitute an additional edifice level.  Program edifice Segues must expose at minimum:  - stable identity; - source level; - target level; - transition type; - ordinal; - lineage; - provenance; - dependencies; - authority state; - extension capacity.  Inverse or convenience views may be projected from this topology.  They must not become duplicate authority.  ## 7. Source-code composition  Source text must remain deterministically decomposable through:  character → line → segment → snippet → script  and reproducible byte-for-byte where the source format permits exact textual reproduction.  A script is therefore not atomic.  A script is an ordered composition of snippet instances.  A snippet is an ordered composition of segment instances.  A segment is an ordered composition of line instances.  A line is an ordered composition of character instances.  ## 8. Upper program composition  Program composition continues without changing architectural grammar:  script → engine → subsystem → system → application  The same instance, composition, Segue, lineage, provenance, dependency, projection, and extension laws apply at every level.  No upper level receives a special ontology merely because its scale is larger.  ## 9. Projection law  Files are projections unless independently established as authority.  Generated program artifacts must be reproducible from their authoritative composition inputs.  A projection may be destroyed and regenerated without destroying its underlying program composition.  Program edifice topology must have one authoritative source.  Derived registries, indexes, maps, manifests, APIs, and runtime constants are projections of that authority.  ## 10. Runtime alignment  The verified runtime implementation projects this edifice through Modus program-composition primitives.  The runtime representation must derive its topology from one shared edifice primitive rather than maintain independent lower-level and upper-level edifice tables.  Runtime implementation does not independently create canon.  It must conform to this authority.  ## 11. Supersession  This decision supersedes, only where inconsistent with the nine-level programming edifice, edifice assertions in:  - `ontology/obelisks/segue/authority_graph/canon/SCRIPT_COMPOSITION_RULE.md` - `ontology/obelisks/segue/authority_graph/canon/UNIVERSAL_INSTANCE_STRUCTURE.md` - `ontology/obelisks/segue/authority_graph/canon/RECURSIVE_COMPOSITION.md` - any historical projection that omits `segment` from the current programming edifice; - any historical programming edifice that substitutes `module`, `service`, `suite`, `estate`, `facet`, `projection`, `runtime`, or another concept for one of these nine canonical programming levels.  Those historical records are not deleted or rewritten.  Their non-conflicting assertions remain unaffected.  ## 12. Preserved doctrine  This decision preserves:  - recursive composition; - instance-first architecture; - Segue-mediated transitions; - canonical substance stored once; - deterministic projection; - immutable history; - lineage preservation; - provenance preservation; - dependency transparency; - extension without replacement.  ## 13. Canonical invariant  The canonical invariant is:  PROGRAM_LEVELS = character → line → segment → snippet → script → engine → subsystem → system → application  PROGRAM_LEVEL_COUNT = 9  ADJACENT_SEGUE_COUNT = 8  TOPOLOGY_AUTHORITY_COUNT = 1  No implementation, projection, compatibility layer, historical document, or convenience registry may weaken or duplicate this authority. ` 
This supersedes rather than destroys the older contradictory assertions, which is required because the old `SCRIPT_COMPOSITION_RULE.md` is explicitly marked `STATUS: CANON / AUTHORITY: USER DIRECTIVE`.  `UNIVERSAL_INSTANCE_STRUCTURE.md` likewise still projects the old `character → line → snippet → script → module → service → application` formulation.
